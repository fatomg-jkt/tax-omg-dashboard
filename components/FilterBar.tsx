export type Filters={tahun:string;masa:string;perusahaan:string;jenis:string;status:string}
export default function FilterBar({filters,setFilters,companies,taxTypes}:{filters:Filters;setFilters:(f:Filters)=>void;companies:string[];taxTypes:string[]}){const set=(k:keyof Filters,v:string)=>setFilters({...filters,[k]:v});return <div className="card grid gap-3 p-4 md:grid-cols-5"><input className="rounded-xl border p-3" placeholder="Tahun" value={filters.tahun} onChange={e=>set('tahun',e.target.value)}/><input className="rounded-xl border p-3" placeholder="Masa Pajak" value={filters.masa} onChange={e=>set('masa',e.target.value)}/><select className="rounded-xl border p-3" value={filters.perusahaan} onChange={e=>set('perusahaan',e.target.value)}><option value="">Semua Perusahaan</option>{companies.map(c=><option key={c}>{c}</option>)}</select><select className="rounded-xl border p-3" value={filters.jenis} onChange={e=>set('jenis',e.target.value)}><option value="">Semua Jenis Pajak</option>{taxTypes.map(t=><option key={t}>{t}</option>)}</select><select className="rounded-xl border p-3" value={filters.status} onChange={e=>set('status',e.target.value)}><option value="">Semua Status</option><option>Sudah Bayar</option><option>Belum memiliki NTPN/NTPD</option><option>Belum Bayar</option></select></div>}
