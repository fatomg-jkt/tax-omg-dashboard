import { useState } from 'react';import { TaxRecord } from '@/utils/excelParser';import { parseNumber } from '@/utils/formatters'
const empty={perusahaan:'',masa_pajak:'',tahun:'2026',jenis_pajak:'',dpp:'',pajak_terhutang:'',ntpn_ntpd:'',status:'Belum memiliki NTPN/NTPD',keterangan:''}
export default function ManualEntryModal({open,onClose,onSave}:{open:boolean;onClose:()=>void;onSave:(r:TaxRecord)=>void}){const[f,setF]=useState(empty);if(!open)return null;const set=(k:keyof typeof empty,v:string)=>setF({...f,[k]:v});function submit(){const ntpn=f.ntpn_ntpd.trim();onSave({id:`manual-${Date.now()}`,perusahaan:f.perusahaan,masa_pajak:f.masa_pajak,tahun:f.tahun,jenis_pajak:f.jenis_pajak,dpp:parseNumber(f.dpp),pajak_terhutang:parseNumber(f.pajak_terhutang),ntpn_ntpd:ntpn,status:ntpn?'Sudah Bayar':f.status,source:'Manual',keterangan:f.keterangan,errors:[]});setF(empty);onClose()}return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4"><div className="card max-h-[90vh] w-full max-w-3xl overflow-auto p-6"><div className="flex justify-between"><h2 className="text-xl font-bold">Tambah Data Pajak Manual</h2><button onClick={onClose}>✕</button></div><div className="mt-5 grid gap-3 md:grid-cols-2">{(['perusahaan','masa_pajak','tahun','jenis_pajak','dpp','pajak_terhutang','ntpn_ntpd','status','keterangan'] as const).map(k=><input key={k} className="rounded-xl border p-3" placeholder={k.replaceAll('_',' ')} value={f[k]} onChange={e=>set(k,e.target.value)}/>)}</div><div className="mt-5 flex justify-end gap-2"><button onClick={onClose} className="rounded-xl border px-4 py-2">Batal</button><button onClick={submit} className="rounded-xl bg-blue-600 px-4 py-2 font-semibold text-white">Simpan</button></div></div></div>}
