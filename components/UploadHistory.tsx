import { UploadHistoryItem } from '@/utils/excelParser'
export default function UploadHistory({history,onView,onDelete}:{history:UploadHistoryItem[];onView:(rows:UploadHistoryItem['data'])=>void;onDelete:(id:string)=>void}){return <div className="card p-5"><h2 className="text-lg font-bold">Riwayat Upload Excel</h2><div className="mt-4 space-y-3">{history.length===0&&<p className="text-sm text-slate-500">Belum ada riwayat upload.</p>}{history.map(h=><div key={h.id} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl bg-slate-50 p-3"><div><p className="font-semibold">{h.fileName}</p><p className="text-sm text-slate-500">{new Date(h.uploadedAt).toLocaleString('id-ID')} • {h.rowCount} baris • {h.errorCount} error</p></div><div className="flex gap-2"><button onClick={()=>onView(h.data)} className="rounded-lg bg-blue-600 px-3 py-2 text-sm font-semibold text-white">Lihat data</button><button onClick={()=>onDelete(h.id)} className="rounded-lg bg-rose-100 px-3 py-2 text-sm font-semibold text-rose-700">Hapus</button></div></div>)}</div></div>}
