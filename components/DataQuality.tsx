import { TaxRecord } from '@/utils/excelParser';import { calculateQuality } from '@/utils/taxCalculations'
export default function DataQuality({rows}:{rows:TaxRecord[]}){const q=calculateQuality(rows);const items=[['Data lengkap',q.complete,'text-emerald-600'],['Data tanpa NTPN/NTPD',q.withoutNtpn,'text-amber-600'],['Data tanpa perusahaan',q.withoutCompany,'text-rose-600'],['Data tanpa jenis pajak',q.withoutTaxType,'text-rose-600'],['Data nominal kosong/0',q.withoutNominal,'text-orange-600']];return <div className="card p-5"><h2 className="text-lg font-bold">Data Quality</h2><div className="mt-4 grid gap-3 md:grid-cols-5">{items.map(([l,v,c])=><div key={l as string} className="rounded-2xl bg-slate-50 p-4"><p className="text-sm text-slate-500">{l}</p><p className={`text-2xl font-bold ${c}`}>{v}</p></div>)}</div></div>}
